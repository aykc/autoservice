.row
  .col
    = render 'shared/error', obj: @order
    %h1 New order
    = turbo_frame_tag "new_order" do
      = render "form", order: @order
    .mb-3
      = link_to "Back to orders", orders_path, class: "btn btn-outline-primary"
